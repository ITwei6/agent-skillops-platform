# Proto IDL

This directory will contain internal RPC contracts for SkillOps services.

The first backend phase only creates the directory layout. Service-specific proto files should be added with the corresponding service implementation.
